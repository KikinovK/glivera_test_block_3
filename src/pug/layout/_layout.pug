include ../templates/_global_vars
include ../templates/_global_mixins
include ../templates/_project_vars
include ../templates/_project_mixins

block vars
block project_vars

doctype
html(lang="en")
	head
		+head(array)
	body(class= array.body_class)
		+header
		section.section(class= array.layout_mod)
			.section_in
				.base
					block content_block

		block local_popup
